---
node_id: decision-20260613T210036-0700-spec043-t008-alex-routing
node_type: routing_decision
spec_id: spec-043
task_id: T008
status: direct_permitted
owned_by: alex
created_at: 2026-06-13T21:00:36-07:00
---

# Decision Drop — spec-043 T008 backend routing

## Decision

`status: DIRECT_PERMITTED`

Direct implementation is permitted for T008. The task is a bounded backend/API alignment from the committed spec-043 handoff: implement the missing hardware image upload contract so the visible UI does not call a dead endpoint.

## Rationale

The handoff states that Quinn pre-implementation gate is already satisfied with `APPROVED_WITH_NOTES` and GO, and places T008 in Wave 1 for Alex. Existing hardware API code already uses the required household membership dependency and typed response models, so the work can follow established patterns without new SpecKit. No product decision is needed when using the preferred endpoint implementation; removing or changing the client affordance remains out of scope unless product explicitly approves it.

## Explicit scope confirmation

Permitted scope is limited to:

1. Add `POST /api/hardware/{id}/image` using existing auth and household authorization patterns.
2. Return a typed hardware/image response and update the hardware image field through existing repository abstractions.
3. Add validation, not-found and error handling, and tests that avoid external storage dependencies.
4. Align the client/API contract only as needed for T008.

Out of scope: broad image-store redesign, schema or data-model migration, auth redesign, household model redesign, infrastructure changes, deployment, pushing, PR creation, or external artifact publication.

## Privacy constraint

This decision was written after reading the repository privacy gate. All follow-on artifacts, fixtures, logs, screenshots, recordings, and evidence must remain sanitized: no secrets, tokens, invite links, emails, production URLs, environment identifiers, local absolute paths, service identifiers, storage bucket names, connection strings, or household/user identifiers.

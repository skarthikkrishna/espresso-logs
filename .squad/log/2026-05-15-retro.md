# Retrospective — 2026-05-15

**Ceremony:** Retrospective with Enforcement  
**Trigger:** No retro log in `.squad/log/` within last 7 days  
**Facilitator:** Tariq  
**Participants:** All squad  
**Date:** 2026-05-15  

---

## 1. What Shipped This Week

### Merged PRs (last 7 days)

| PR | Title | Status |
|----|-------|--------|
| #62 | feat: M4 read switchover — route reads to PostgreSQL when USE_POSTGRES=true | Open (CI failures fixed locally; branch not pushed) |
| #61 | chore(copilot): sync .copilot skills + harden copilot-instructions | ✅ Merged |
| #60 | fix(ui): Safari rendering fixes + Playwright E2E suite [spec-029] | ✅ Merged |

### Closed Work (by topic)

**M4 Read Switchover Prerequisites (P1 + P2)**
- ✅ All 5 ORM models updated with migration-0004 columns
- ✅ Migration 0005 ready (alembic — not committed; see blocker)
- ✅ All 5 SQL repos rewritten with upsert-by-sheets_id pattern + v2 column writes
- ✅ All 5 SQL repos: async `list()`, `get()`, entity-specific read methods
- ✅ All 5 DualWrite wrappers: async read methods with `use_postgres` check
- ✅ All 6 routers + 2 services: `await` all repo read calls
- ✅ 27 new tests in `test_sql_repos_read.py` covering both read paths
- ✅ 399 tests passing, 4 skipped, 0 lint errors (local)

**UI / Design System**
- ✅ Chip component unified: single amber frosted-glass style, 5 call sites updated (Aria + Finn)
- ✅ Safari rendering fixes: rounded corners, backdrop blur, z-index polish
- ✅ Playwright E2E suite added [spec-029]

**Backend Security**
- ✅ E2E auth bypass production guard: `E2E_AUTH_BYPASS=1` hard-fails on non-test environments
- ✅ Thread safety: E2E_SEED schema alignment, public `delete_by_pk` on BaseRepo
- ✅ Abstract method policy (DEC-M01): `delete_rows` declared on BaseRepo

**Governance / Process**
- ✅ Ceremony enforcement (retro-enforcement skill) added to ceremonies.md
- ✅ Binary push gate codified (ask or pause — no other options)
- ✅ Coordinator CI discipline: Tariq triage mandatory on build failure before any fix

---

## 2. What Did Not Ship

**feat/m4-prerequisites (PR #62)**
- Branch has 7 commits staged locally — **not pushed**
- Quinn gate 🔴 BLOCKED on 3 findings (see Section 3)
- CI failures were diagnosed (RCA written) and locally fixed, but not yet verified in CI
- Push gated on: Karthik P3 backfill decision + Quinn re-gate approval

**Open Blockers (from now.md)**
1. Migration 0005 alembic artifact — not generated/committed
2. Write-then-read integration test — not written
3. M4 P3 backfill scope — awaiting Karthik decision

---

## 3. Root Cause on Failures

### CI Failure — PR #62 (feat/m4-prerequisites)

Source: `.squad/log/2026-05-14-m4-ci-rca.md`

**3 of 13 CI checks failed:** format, typecheck, test

**Root causes (ordered by severity):**

| # | Root Cause | Impact |
|---|-----------|--------|
| RC-1 | Async read path added to SQL repos but Sheets repos and DualWrite wrappers left sync | 14/19 mypy errors; 16 test failures in CI |
| RC-2 | SQLAlchemy column types used as Python types without cast | 4/19 mypy errors |
| RC-3 | Test stubs call async methods without `await` (local skip hid this; CI has DATABASE_URL set) | 16 test failures only visible in CI |
| RC-4 | `uv run mypy app/ --strict` not run locally before push | Process failure — all errors catchable pre-push |
| RC-5 | `uv run ruff format` not run before push | 5 format violations in 5 files |

**Pattern:** Incomplete migration from sync → async interface. SQL repos made async; callers, wrappers, Sheets repos, and tests left unchanged. CI exercised the full async path (DATABASE_URL set); local dev silently skipped it.

**Fix status:** Phases 1–4 of the fix plan were implemented in `b5a5bff`. Branch not pushed pending Quinn re-gate.

---

## 4. Action Items (GitHub Issues)

All action items created as GitHub Issues labeled `retro-action`:

| Issue | Title | Source |
|-------|-------|--------|
| [#63](https://github.com/skarthikkrishna/espresso-logs/issues/63) | [M4] Generate alembic migration 0005 for sheets_hardware_id column | Quinn gate blocker #1 |
| [#64](https://github.com/skarthikkrishna/espresso-logs/issues/64) | [M4] Add SQL write-then-read integration test for postgres read path | Quinn gate blocker #2 |
| [#65](https://github.com/skarthikkrishna/espresso-logs/issues/65) | [M4] Operator decision required: M4 P3 backfill scope | Quinn gate blocker #3 |
| [#66](https://github.com/skarthikkrishna/espresso-logs/issues/66) | [Process] Add pre-push check script to catch mypy-strict and ruff failures before CI | Process failure (RC-4 + RC-5) |

---

## 5. Rework Rate

**Command attempted:** `npx github:bradygaster/squad rework`  
**Result:** `npm error could not determine executable to run` — package not available in this environment.  
**Skip reason:** `@bradygaster/squad` is not installed or published as a runnable npx package. Command skipped gracefully per ceremony spec.

**Manual rework signal (from git log):**
- PR #62 required a follow-up fix commit (`b5a5bff`) to resolve CI failures introduced by the initial implementation. This is a rework event.
- Estimated rework contribution from M4 branch: 1 fix commit out of ~8 feature commits ≈ 12.5% (within the ≤15% healthy threshold, but borderline given the CI failures were process-preventable).
- No formal metric available without the rework tool.

---

## Health Summary

| Signal | Status |
|--------|--------|
| Shipped features | ✅ Healthy — UI polish, E2E suite, auth guard, governance all landed |
| M4 P1+P2 | ✅ Complete locally; push blocked by process gates |
| Quinn gate | 🔴 Blocked — 3 open findings |
| CI discipline | 🟡 One preventable failure event; process issue captured in Issue #66 |
| Rework rate | 🟡 ~12.5% estimated (borderline; tool unavailable for precise measurement) |
| Open blockers | 🔴 P3 backfill decision (Karthik) blocks all M4 forward progress |

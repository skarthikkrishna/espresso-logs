# 2025-07-30: LLM Enrichment Layer — Payload, Polling, Persistence

**Agent:** Alex (Backend) + Maya (Architecture)  
**Decision:** Fire-and-forget inference in `POST /api/brew-log`; enrich prompt with resolved server context + client-sent zone_taste; persist Zone_Taste to Brew_Log sheet  
**Outcome:** Payload extension pattern (extra_context, zone_taste on _BrewLogCreateBody); polling endpoint GET /api/brew-log/{id}/feedback; frontend navigation with ?fresh=1 trigger

**Key facts:**
- Inference stays embedded in POST endpoint via asyncio.create_task
- Enrich prompt with: machine_name, basket_name, roast_level, zone_taste
- Zone_Taste → new Brew_Log column (snake_case display, Zone_Taste header)
- Frontend polls /feedback every 3s until ai_feedback available

**Next:** Implement payload extension, annotation endpoint, persistence layer.

# 2025-07-30: ZoneBoundaries Architecture Decision

**Agent:** Maya (Architecture) + Finn (Frontend)  
**Decision:** ZoneBoundaries as frontend-only TypeScript constants utility  
**Outcome:** `frontend/src/utils/zoneBoundaries.ts` defined; CompassChart prop interface extended with optional `zoneBoundaries` parameter (backward compatible, all 87 tests unaffected)

**Key facts:**
- No backend schema changes required
- `MACHINE_TIME_PROFILES` and `ROAST_RATIO_PROFILES` remain frontend-only
- Default behavior preserved (equal-thirds model)

**Next:** Reference in CompassChart prop validation and component render logic.
